PORT=8000

# Optional GitHub integration.
# Use a fine-grained GitHub token with only the repo permissions you need.
GITHUB_TOKEN=
GITHUB_OWNER=
GITHUB_REPO=

# Local git worktree that pull/push should operate on.
GIT_WORKTREE=D:\MICRO SOFT\collabflow

# Optional future AI integration.
GEMINI_API_KEY=
